{
  "name": "kalender",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "set NODE_ENV=test && mocha --recursive",
    "test-watch": "set NODE_ENV=test && nodemon --exec mocha --recursive",
    "test-watch-min": "set NODE_ENV=test && nodemon --exec mocha --recursive -R min"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/somsjan/kalender.git"
  },
  "author": "Jan v/d Molen",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/somsjan/kalender/issues"
  },
  "homepage": "https://github.com/somsjan/kalender#readme",
  "devDependencies": {
    "mocha": "^3.5.0",
    "supertest": "^3.0.0"
  },
  "dependencies": {
    "body-parser": "^1.17.2",
    "express": "^4.15.3",
    "express-validator": "^3.2.1",
    "mongoose": "^4.10.8"
  }
}
